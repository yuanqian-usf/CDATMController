package com.circulardollar.cdatm.utils;

import com.circulardollar.cdatm.business.downstream.model.account.IAccount;
import java.util.ArrayList;
import java.util.List;

public class MockGenerators {
  List<IAccount> createRandomAccounts(int size) {
    List<IAccount> mockAccounts = new ArrayList<>();
    //    for (int i = 0; i < size; i++) {
    //      mockAccounts.add(Account.newBuilder().setAccountNumber())
    //    }
    //  }
    return null;
  }
}
